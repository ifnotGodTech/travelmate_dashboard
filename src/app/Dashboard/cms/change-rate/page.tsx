import Addrate from "@/components/pages/cms/Addrate";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <Addrate />
    </div>
  );
};

export default page;
